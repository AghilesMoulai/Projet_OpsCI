PORT=3000
NODE_ENV=development

# Option 1: URL unique, pratique pour Docker / VM / hebergeurs manages
# DATABASE_URL=postgresql://user:change-me@host/dbname?sslmode=require

# Option 2: variables séparées
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cinematheque
DB_USER=postgres
DB_PASSWORD=password
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=false

SECRET_KEY=change-me-in-production
FRONTEND_URL=http://localhost:5173
